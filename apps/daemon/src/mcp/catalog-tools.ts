// MCP catalog tools (skills, design-systems exposed as tools rather than resources).
// See docs/internal/mcp-write-design.md §1.
// TODO(HRG-18): implement.

import type { McpContext, McpToolRegistry } from './index.js';

export function registerCatalogTools(_registry: McpToolRegistry, _ctx: McpContext): void {
  // Intentionally empty. Tools registered in HRG-18.
}
